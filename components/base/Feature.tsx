// components/sections/FeatureSection.tsx
import React from 'react';

type FeatureSectionProps = {
  title: string;
  description: string;
  imageSrc: string;
};

const FeatureSection: React.FC<FeatureSectionProps> = ({ title, description, imageSrc }) => (
  <section>
    {/* Your feature section implementation */}
  </section>
);

export default FeatureSection;
